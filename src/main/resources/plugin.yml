name: ${plugin.name}
version: ${plugin.vers}
main: ${plugin.pckg}.internal.${plugin.name}BukkitPlugin
authors: ["${plugin.athr}"]
folia-supported: true
api-version: 1.13
load: STARTUP

softdepend: ${plugin.deps}